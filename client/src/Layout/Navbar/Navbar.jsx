import styles from './Navbar.module.css';

const Navbar = () => {
    return (
        <div id='navbar' className={styles['site-navbar']}>
            Navbar
        </div>
    );
};

export default Navbar;
